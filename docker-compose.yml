services:
  pocketbase:
    image: ghcr.io/s4ndyp/tinynote:latest
    container_name: tinynote
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - tinynote_data:/pb/pb_data
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/api/health"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 10s

volumes:
  tinynote_data:
